import React from 'react';
import VideoPreview from '../../components/VideoPreview/VideoPreview';
import './Home.scss';

const Home = (props) => {
	return (
		<div className='home'>
			<VideoPreview />
		</div>
	);
};

export default Home;
